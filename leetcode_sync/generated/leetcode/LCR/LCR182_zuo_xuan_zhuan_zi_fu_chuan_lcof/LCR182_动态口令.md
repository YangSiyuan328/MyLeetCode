# LCR 182. 动态口令

<table>
<tr><td><b>难度</b></td><td>Easy</td></tr>
<tr><td><b>标签</b></td><td>数学;双指针;字符串</td></tr>
<tr><td><b>会员</b></td><td>否</td></tr>
<tr><td><b>链接</b></td><td><a href="https://leetcode.cn/problems/zuo-xuan-zhuan-zi-fu-chuan-lcof/description/">在线练习</a></td></tr>
</table>

## 题目描述

某公司门禁密码使用动态口令技术。初始密码为字符串 `password`，密码更新均遵循以下步骤：
  * 设定一个正整数目标值 `target`
  * 将 `password` 前 `target` 个字符按原顺序移动至字符串末尾
请返回更新后的密码字符串。

**示例 1：**

**输入:** password = "s3cur1tyC0d3", target = 4

**输出:** "r1tyC0d3s3cu"

**示例 2：**

**输入:** password = "lrloseumgh", target = 6

**输出:  **"umghlrlose"

**提示：**
  * `1 <= target < password.length <= 10000`

## 示例

```
"s3cur1tyC0d3"
4
"vbzkgsaoiu"
2
```

## 参考答案

```java
class Solution {
    public String dynamicPassword(String password, int target) {
        
    }
}
```

## 一刷思路

```java
class Solution {
    public String dynamicPassword(String password, int target) {
        
    }
}
```

## 二刷思路

```java
class Solution {
    public String dynamicPassword(String password, int target) {
        
    }
}
```
