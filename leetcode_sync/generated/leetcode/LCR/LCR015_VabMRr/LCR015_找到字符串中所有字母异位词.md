# LCR 015. 找到字符串中所有字母异位词

<table>
<tr><td><b>难度</b></td><td>Medium</td></tr>
<tr><td><b>标签</b></td><td>哈希表;字符串;滑动窗口</td></tr>
<tr><td><b>会员</b></td><td>否</td></tr>
<tr><td><b>链接</b></td><td><a href="https://leetcode.cn/problems/VabMRr/description/">在线练习</a></td></tr>
</table>

## 题目描述

给定两个字符串 `s` 和** **`p`，找到 `s`** ** 中所有 `p` 的 **变位词  **的子串，返回这些子串的起始索引。不考虑答案输出的顺序。

**变位词** 指字母相同，但排列不同的字符串。

**示例 1：**

**输入:** s = "cbaebabacd", p = "abc"

**输出:**[0,6]

**解释:**
起始索引等于 0 的子串是 "cba", 它是 "abc" 的变位词。
起始索引等于 6 的子串是 "bac", 它是 "abc" 的变位词。

**  示例 2：**

**输入:** s = "abab", p = "ab"

**输出:**[0,1,2]

**解释:**
起始索引等于 0 的子串是 "ab", 它是 "ab" 的变位词。
起始索引等于 1 的子串是 "ba", 它是 "ab" 的变位词。
起始索引等于 2 的子串是 "ab", 它是 "ab" 的变位词。

**提示：**
  * `1 <= s.length, p.length <= 3 * 104`
  * `s` 和 `p` 仅包含小写字母
注意：本题与主站 438 题相同： <https://leetcode.cn/problems/find-all-anagrams-in-a-string/>

## 示例

```
"cbaebabacd"
"abc"
```

## 参考答案

```java
class Solution {
    public List<Integer> findAnagrams(String s, String p) {

    }
}
```

## 一刷思路

```java
class Solution {
    public List<Integer> findAnagrams(String s, String p) {

    }
}
```

## 二刷思路

```java
class Solution {
    public List<Integer> findAnagrams(String s, String p) {

    }
}
```
