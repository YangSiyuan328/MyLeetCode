# LCR 186. 文物朝代判断

<table>
<tr><td><b>难度</b></td><td>Easy</td></tr>
<tr><td><b>标签</b></td><td>数组;排序</td></tr>
<tr><td><b>会员</b></td><td>否</td></tr>
<tr><td><b>链接</b></td><td><a href="https://leetcode.cn/problems/bu-ke-pai-zhong-de-shun-zi-lcof/description/">在线练习</a></td></tr>
</table>

## 题目描述

展览馆展出来自 13 个朝代的文物，每排展柜展出 5 个文物。某排文物的摆放情况记录于数组 `places`，其中 `places[i]` 表示处于第 `i` 位文物的所属朝代编号。其中，编号为 0 的朝代表示未知朝代。请判断并返回这排文物的所属朝代编号是否能够视为连续的五个朝代（如遇未知朝代可算作连续情况）。

**示例 1：**

**输入：** places = [0, 6, 9, 0, 7]

**输出：** True

**示例  2：**

**输入：** places = [7, 8, 9, 10, 11]

**输出：** True

**提示：**
  * `places.length = 5`
  * `0 <= places[i] <= 13`

## 示例

```
[0,6,9,0,7]
```

## 参考答案

```java
class Solution {
    public boolean checkDynasty(int[] places) {
        
    }
}
```

## 一刷思路

```java
class Solution {
    public boolean checkDynasty(int[] places) {
        
    }
}
```

## 二刷思路

```java
class Solution {
    public boolean checkDynasty(int[] places) {
        
    }
}
```
