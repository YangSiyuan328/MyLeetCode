# 777. 在 LR 字符串中交换相邻字符

<table>
<tr><td><b>难度</b></td><td>Medium</td></tr>
<tr><td><b>标签</b></td><td>双指针;字符串</td></tr>
<tr><td><b>会员</b></td><td>否</td></tr>
<tr><td><b>链接</b></td><td><a href="https://leetcode.cn/problems/swap-adjacent-in-lr-string/description/">在线练习</a></td></tr>
</table>

## 题目描述

在一个由 `'L'` , `'R'` 和 `'X'` 三个字符组成的字符串（例如`"RXXLRXRXL"`）中进行移动操作。一次移动操作指用一个 `"LX"` 替换一个 `"XL"`，或者用一个 `"XR"` 替换一个 `"RX"`。现给定起始字符串 `start` 和结束字符串 `result`，请编写代码，当且仅当存在一系列移动操作使得 `start` 可以转换成 `result` 时， 返回 `True`。

**示例 1：**

**输入：** start = "RXXLRXRXL", result = "XRLXXRRLX"

**输出：** true

**解释：** 通过以下步骤我们可以将 start 转化为 result：
RXXLRXRXL ->
XRXLRXRXL ->
XRLXRXRXL ->
XRLXXRRXL ->
XRLXXRRLX

**示例 2：**

**输入：** start = "X", result = "L"

**输出：** false

**提示：**
  * `1 <= start.length <= 104`
  * `start.length == result.length`
  * `start` 和 `result` 都只包含 `'L'`, `'R'` 或 `'X'`。

## 示例

```
"RXXLRXRXL"
"XRLXXRRLX"
"X"
"L"
```

## 参考答案

```java
class Solution {
    public boolean canTransform(String start, String result) {
        
    }
}
```

## 一刷思路

```java
class Solution {
    public boolean canTransform(String start, String result) {
        
    }
}
```

## 二刷思路

```java
class Solution {
    public boolean canTransform(String start, String result) {
        
    }
}
```
