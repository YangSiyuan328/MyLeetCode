# 906. 超级回文数

<table>
<tr><td><b>难度</b></td><td>Hard</td></tr>
<tr><td><b>标签</b></td><td>数学;字符串;枚举</td></tr>
<tr><td><b>会员</b></td><td>否</td></tr>
<tr><td><b>链接</b></td><td><a href="https://leetcode.cn/problems/super-palindromes/description/">在线练习</a></td></tr>
</table>

## 题目描述

如果一个正整数自身是回文数，而且它也是一个回文数的平方，那么我们称这个数为 **超级回文数** 。
现在，给你两个以字符串形式表示的正整数 left 和 right  ，统计并返回区间 `[left, right]` 中的 **超级回文数** 的数目。

**示例 1：**

**输入：** left = "4", right = "1000"

**输出：** 4

**解释：** 4、9、121 和 484 都是超级回文数。
注意 676 不是超级回文数：26 * 26 = 676 ，但是 26 不是回文数。

**示例 2：**

**输入：** left = "1", right = "2"

**输出：** 1

**提示：**
  * `1 <= left.length, right.length <= 18`
  * `left` 和 `right` 仅由数字（0 - 9）组成。
  * `left` 和 `right` 不含前导零。
  * `left` 和 `right` 表示的整数在区间 `[1, 1018 - 1]` 内。
  * `left` 小于等于 `right` 。

## 示例

```
"4"
"1000"
"1"
"2"
```

## 参考答案

```java
class Solution {
    public int superpalindromesInRange(String left, String right) {
        
    }
}
```

## 一刷思路

```java
class Solution {
    public int superpalindromesInRange(String left, String right) {
        
    }
}
```

## 二刷思路

```java
class Solution {
    public int superpalindromesInRange(String left, String right) {
        
    }
}
```
