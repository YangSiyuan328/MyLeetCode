# 862. 和至少为 K 的最短子数组

<table>
<tr><td><b>难度</b></td><td>Hard</td></tr>
<tr><td><b>标签</b></td><td>队列;数组;二分查找;前缀和;滑动窗口;单调队列;堆（优先队列）</td></tr>
<tr><td><b>会员</b></td><td>否</td></tr>
<tr><td><b>链接</b></td><td><a href="https://leetcode.cn/problems/shortest-subarray-with-sum-at-least-k/description/">在线练习</a></td></tr>
</table>

## 题目描述

给你一个整数数组 `nums` 和一个整数 `k` ，找出 `nums` 中和至少为 `k` 的 **最短非空子数组** ，并返回该子数组的长度。如果不存在这样的 **子数组** ，返回 `-1` 。

**子数组** 是数组中 **连续** 的一部分。

**示例 1：**

**输入：** nums = [1], k = 1

**输出：** 1

**示例 2：**

**输入：** nums = [1,2], k = 4

**输出：** -1

**示例 3：**

**输入：** nums = [2,-1,2], k = 3

**输出：** 3

**提示：**
  * `1 <= nums.length <= 105`
  * `-105 <= nums[i] <= 105`
  * `1 <= k <= 109`

## 示例

```
[1]
1
[1,2]
4
[2,-1,2]
3
```

## 参考答案

```java
class Solution {
    public int shortestSubarray(int[] nums, int k) {
        
    }
}
```

## 一刷思路

```java
class Solution {
    public int shortestSubarray(int[] nums, int k) {
        
    }
}
```

## 二刷思路

```java
class Solution {
    public int shortestSubarray(int[] nums, int k) {
        
    }
}
```
