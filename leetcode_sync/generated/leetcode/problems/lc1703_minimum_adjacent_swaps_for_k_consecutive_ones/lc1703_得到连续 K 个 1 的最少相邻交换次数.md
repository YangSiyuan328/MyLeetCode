# 1703. 得到连续 K 个 1 的最少相邻交换次数

<table>
<tr><td><b>难度</b></td><td>Hard</td></tr>
<tr><td><b>标签</b></td><td>贪心;数组;前缀和;滑动窗口</td></tr>
<tr><td><b>会员</b></td><td>否</td></tr>
<tr><td><b>链接</b></td><td><a href="https://leetcode.cn/problems/minimum-adjacent-swaps-for-k-consecutive-ones/description/">在线练习</a></td></tr>
</table>

## 题目描述

给你一个整数数组 `nums` 和一个整数 `k` 。 `nums` 仅包含 `0` 和 `1` 。每一次移动，你可以选择 **相邻** 两个数字并将它们交换。
请你返回使 `nums` 中包含 `k` 个 **连续**`1` 的 **最少** 交换次数。

**示例 1：**

**输入：** nums = [1,0,0,1,0,1], k = 2

**输出：** 1

**解释：** 在第一次操作时，nums 可以变成 [1,0,0,0,**1** ,**1**] 得到连续两个 1 。

**示例 2：**

**输入：** nums = [1,0,0,0,0,0,1,1], k = 3

**输出：** 5

**解释：** 通过 5 次操作，最左边的 1 可以移到右边直到 nums 变为 [0,0,0,0,0,**1** ,**1** ,**1**] 。

**示例 3：**

**输入：** nums = [1,1,0,1], k = 2

**输出：** 0

**解释：** nums 已经有连续 2 个 1 了。

**提示：**
  * `1 <= nums.length <= 105`
  * `nums[i]` 要么是 `0` ，要么是 `1` 。
  * `1 <= k <= sum(nums)`

## 示例

```
[1,0,0,1,0,1]
2
[1,0,0,0,0,0,1,1]
3
[1,1,0,1]
2
```

## 参考答案

```java
class Solution {
    public int minMoves(int[] nums, int k) {
        
    }
}
```

## 一刷思路

```java
class Solution {
    public int minMoves(int[] nums, int k) {
        
    }
}
```

## 二刷思路

```java
class Solution {
    public int minMoves(int[] nums, int k) {
        
    }
}
```
