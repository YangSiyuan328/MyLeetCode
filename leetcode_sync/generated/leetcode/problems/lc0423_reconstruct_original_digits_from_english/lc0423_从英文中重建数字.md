# 423. 从英文中重建数字

<table>
<tr><td><b>难度</b></td><td>Medium</td></tr>
<tr><td><b>标签</b></td><td>哈希表;数学;字符串</td></tr>
<tr><td><b>会员</b></td><td>否</td></tr>
<tr><td><b>链接</b></td><td><a href="https://leetcode.cn/problems/reconstruct-original-digits-from-english/description/">在线练习</a></td></tr>
</table>

## 题目描述

给你一个字符串 `s` ，其中包含字母顺序打乱的用英文单词表示的若干数字（`0-9`）。按 **升序** 返回原始的数字。

**示例 1：**

**输入：** s = "owoztneoer"

**输出：** "012"

**示例 2：**

**输入：** s = "fviefuro"

**输出：** "45"

**提示：**
  * `1 <= s.length <= 105`
  * `s[i]` 为 `["e","g","f","i","h","o","n","s","r","u","t","w","v","x","z"]` 这些字符之一
  * `s` 保证是一个符合题目要求的字符串

## 示例

```
"owoztneoer"
"fviefuro"
```

## 参考答案

```java
class Solution {
    public String originalDigits(String s) {
        
    }
}
```

## 一刷思路

```java
class Solution {
    public String originalDigits(String s) {
        
    }
}
```

## 二刷思路

```java
class Solution {
    public String originalDigits(String s) {
        
    }
}
```
