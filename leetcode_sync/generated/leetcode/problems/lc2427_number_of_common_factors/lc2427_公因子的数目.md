# 2427. 公因子的数目

<table>
<tr><td><b>难度</b></td><td>Easy</td></tr>
<tr><td><b>标签</b></td><td>数学;枚举;数论</td></tr>
<tr><td><b>会员</b></td><td>否</td></tr>
<tr><td><b>链接</b></td><td><a href="https://leetcode.cn/problems/number-of-common-factors/description/">在线练习</a></td></tr>
</table>

## 题目描述

给你两个正整数 `a` 和 `b` ，返回 `a` 和 `b` 的 **公** 因子的数目。
如果 `x` 可以同时整除 `a` 和 `b` ，则认为 `x` 是 `a` 和 `b` 的一个 **公因子** 。

**示例 1：**

**输入：** a = 12, b = 6

**输出：** 4

**解释：** 12 和 6 的公因子是 1、2、3、6 。

**示例 2：**

**输入：** a = 25, b = 30

**输出：** 2

**解释：** 25 和 30 的公因子是 1、5 。

**提示：**
  * `1 <= a, b <= 1000`

## 示例

```
12
6
25
30
```

## 参考答案

```java
class Solution {
    public int commonFactors(int a, int b) {
        
    }
}
```

## 一刷思路

```java
class Solution {
    public int commonFactors(int a, int b) {
        
    }
}
```

## 二刷思路

```java
class Solution {
    public int commonFactors(int a, int b) {
        
    }
}
```
