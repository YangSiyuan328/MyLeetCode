# 20. 有效的括号

<table>
<tr><td><b>难度</b></td><td>Easy</td></tr>
<tr><td><b>标签</b></td><td>栈;字符串</td></tr>
<tr><td><b>会员</b></td><td>否</td></tr>
<tr><td><b>链接</b></td><td><a href="https://leetcode.cn/problems/valid-parentheses/description/">在线练习</a></td></tr>
</table>

## 题目描述

给定一个只包括 `'('`，`')'`，`'{'`，`'}'`，`'['`，`']'` 的字符串 `s` ，判断字符串是否有效。
有效字符串需满足：
  1. 左括号必须用相同类型的右括号闭合。
  2. 左括号必须以正确的顺序闭合。
  3. 每个右括号都有一个对应的相同类型的左括号。

**示例 1：**

**输入：** s = "()"

**输出：** true

**示例 2：**

**输入：** s = "()[]{}"

**输出：** true

**示例 3：**

**输入：** s = "(]"

**输出：** false

**示例 4：**

**输入：** s = "([])"

**输出：** true

**示例 5：**

**输入：** s = "([)]"

**输出：** false

**提示：**
  * `1 <= s.length <= 104`
  * `s` 仅由括号 `'()[]{}'` 组成

## 示例

```
"()"
"()[]{}"
"(]"
"([])"
"([)]"
```

## 参考答案

```java
class Solution {
    public boolean isValid(String s) {
        
    }
}
```

## 一刷思路

```java
class Solution {
    public boolean isValid(String s) {
        
    }
}
```

## 二刷思路

```java
class Solution {
    public boolean isValid(String s) {
        
    }
}
```
