# 1044. 最长重复子串

<table>
<tr><td><b>难度</b></td><td>Hard</td></tr>
<tr><td><b>标签</b></td><td>字符串;二分查找;后缀数组;滑动窗口;哈希函数;滚动哈希</td></tr>
<tr><td><b>会员</b></td><td>否</td></tr>
<tr><td><b>链接</b></td><td><a href="https://leetcode.cn/problems/longest-duplicate-substring/description/">在线练习</a></td></tr>
</table>

## 题目描述

给你一个字符串 `s` ，考虑其所有 _重复子串_ ：即 `s` 的（连续）子串，在 `s` 中出现 2 次或更多次。这些出现之间可能存在重叠。
返回 **任意一个** 可能具有最长长度的重复子串。如果 `s` 不含重复子串，那么答案为 `""` 。

**示例 1：**

**输入：** s = "banana"

**输出：** "ana"

**示例 2：**

**输入：** s = "abcd"

**输出：** ""

**提示：**
  * `2 <= s.length <= 3 * 104`
  * `s` 由小写英文字母组成

## 示例

```
"banana"
"abcd"
```

## 参考答案

```java
class Solution {
    public String longestDupSubstring(String s) {
        
    }
}
```

## 一刷思路

```java
class Solution {
    public String longestDupSubstring(String s) {
        
    }
}
```

## 二刷思路

```java
class Solution {
    public String longestDupSubstring(String s) {
        
    }
}
```
