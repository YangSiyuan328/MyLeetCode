# 5. 最长回文子串

<table>
<tr><td><b>难度</b></td><td>Medium</td></tr>
<tr><td><b>标签</b></td><td>双指针;字符串;动态规划</td></tr>
<tr><td><b>会员</b></td><td>否</td></tr>
<tr><td><b>链接</b></td><td><a href="https://leetcode.cn/problems/longest-palindromic-substring/description/">在线练习</a></td></tr>
</table>

## 题目描述

给你一个字符串 `s`，找到 `s` 中最长的 回文 子串。

**示例 1：**

**输入：** s = "babad"

**输出：** "bab"

**解释：** "aba" 同样是符合题意的答案。

**示例 2：**

**输入：** s = "cbbd"

**输出：** "bb"

**提示：**
  * `1 <= s.length <= 1000`
  * `s` 仅由数字和英文字母组成

## 示例

```
"babad"
"cbbd"
```

## 参考答案

```java
class Solution {
    public String longestPalindrome(String s) {
        
    }
}
```

## 一刷思路

```java
class Solution {
    public String longestPalindrome(String s) {
        
    }
}
```

## 二刷思路

```java
class Solution {
    public String longestPalindrome(String s) {
        
    }
}
```
