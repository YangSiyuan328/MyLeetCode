# 34. 在排序数组中查找元素的第一个和最后一个位置

<table>
<tr><td><b>难度</b></td><td>Medium</td></tr>
<tr><td><b>标签</b></td><td>数组;二分查找</td></tr>
<tr><td><b>会员</b></td><td>否</td></tr>
<tr><td><b>链接</b></td><td><a href="https://leetcode.cn/problems/find-first-and-last-position-of-element-in-sorted-array/description/">在线练习</a></td></tr>
</table>

## 题目描述

给你一个按照非递减顺序排列的整数数组 `nums`，和一个目标值 `target`。请你找出给定目标值在数组中的开始位置和结束位置。
如果数组中不存在目标值 `target`，返回 `[-1, -1]`。
你必须设计并实现时间复杂度为 `O(log n)` 的算法解决此问题。

**示例 1：**

**输入：** nums = [5,7,7,8,8,10], target = 8

**输出：**[3,4]

**示例  2：**

**输入：** nums = [5,7,7,8,8,10], target = 6

**输出：**[-1,-1]

**示例 3：**

**输入：** nums = [], target = 0

**输出：**[-1,-1]

**提示：**
  * `0 <= nums.length <= 105`
  * `-109 <= nums[i] <= 109`
  * `nums` 是一个非递减数组
  * `-109 <= target <= 109`

## 示例

```
[5,7,7,8,8,10]
8
[5,7,7,8,8,10]
6
[]
0
```

## 参考答案

```java
class Solution {
    public int[] searchRange(int[] nums, int target) {
        
    }
}
```

## 一刷思路

```java
class Solution {
    public int[] searchRange(int[] nums, int target) {
        
    }
}
```

## 二刷思路

```java
class Solution {
    public int[] searchRange(int[] nums, int target) {
        
    }
}
```
