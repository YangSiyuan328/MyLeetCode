# 3750. 最少翻转次数得到反转二进制字符串

<table>
<tr><td><b>难度</b></td><td>Easy</td></tr>
<tr><td><b>标签</b></td><td>位运算;数学;双指针;字符串</td></tr>
<tr><td><b>会员</b></td><td>否</td></tr>
<tr><td><b>链接</b></td><td><a href="https://leetcode.cn/problems/minimum-number-of-flips-to-reverse-binary-string/description/">在线练习</a></td></tr>
</table>

## 题目描述

给你一个 **正** 整数 `n`。
令 `s` 为 `n` 的 **二进制表示** （不含前导零）。
二进制字符串 `s` 的 **反转** 是指将 `s` 中的字符按相反顺序排列得到的字符串。
你可以翻转 `s` 中的任意一位（将 `0 → 1` 或 `1 → 0`）。每次翻转 **仅** 影响一位。
请返回使 `s` 等于其原始形式的反转所需的 **最少** 翻转次数。

**示例 1：**

**输入：** n = 7

**输出：** 0

**解释：**
7 的二进制表示为 `"111"`。其反转也是 `"111"`，两者相同。因此，不需要翻转。

**示例 2：**

**输入：** n = 10

**输出：** 4

**解释：**
10 的二进制表示为 `"1010"`。其反转为 `"0101"`。必须翻转所有四位才能使它们相等。因此，所需的最少翻转次数为 4。

**提示：**
  * `1 <= n <= 109`

## 示例

```
7
10
```

## 参考答案

```java
class Solution {
    public int minimumFlips(int n) {
        
    }
}
```

## 一刷思路

```java
class Solution {
    public int minimumFlips(int n) {
        
    }
}
```

## 二刷思路

```java
class Solution {
    public int minimumFlips(int n) {
        
    }
}
```
