# 805. 数组的均值分割

<table>
<tr><td><b>难度</b></td><td>Hard</td></tr>
<tr><td><b>标签</b></td><td>位运算;数组;哈希表;数学;动态规划;位掩码</td></tr>
<tr><td><b>会员</b></td><td>否</td></tr>
<tr><td><b>链接</b></td><td><a href="https://leetcode.cn/problems/split-array-with-same-average/description/">在线练习</a></td></tr>
</table>

## 题目描述

给定你一个整数数组 `nums`
我们要将 `nums` 数组中的每个元素移动到 `A` 数组 或者 `B` 数组中，使得 `A` 数组和 `B` 数组不为空，并且 `average(A) == average(B)` 。
如果可以完成则返回`true` ， 否则返回 `false`  。

**注意：** 对于数组 `arr` ,  `average(arr)` 是 `arr` 的所有元素的和除以 `arr` 长度。

**示例 1:**

**输入:** nums = [1,2,3,4,5,6,7,8]

**输出:** true

**解释:** 我们可以将数组分割为 [1,4,5,8] 和 [2,3,6,7], 他们的平均值都是4.5。

**示例 2:**

**输入:** nums = [3,1]

**输出:** false

**提示:**
  * `1 <= nums.length <= 30`
  * `0 <= nums[i] <= 104`

## 示例

```
[1,2,3,4,5,6,7,8]
[3,1]
```

## 参考答案

```java
class Solution {
    public boolean splitArraySameAverage(int[] nums) {
        
    }
}
```

## 一刷思路

```java
class Solution {
    public boolean splitArraySameAverage(int[] nums) {
        
    }
}
```

## 二刷思路

```java
class Solution {
    public boolean splitArraySameAverage(int[] nums) {
        
    }
}
```
