# 3759. 统计合格元素的数目

<table>
<tr><td><b>难度</b></td><td>Medium</td></tr>
<tr><td><b>标签</b></td><td>数组;二分查找;分治;快速选择;排序</td></tr>
<tr><td><b>会员</b></td><td>否</td></tr>
<tr><td><b>链接</b></td><td><a href="https://leetcode.cn/problems/count-elements-with-at-least-k-greater-values/description/">在线练习</a></td></tr>
</table>

## 题目描述

给你一个长度为 `n` 的整数数组 `nums` 和一个整数 `k`。
如果数组 `nums` 中的某个元素满足以下条件，则称其为 **合格元素** ：存在 **至少** `k` 个元素 **严格大于  **它。
返回一个整数，表示数组 `nums` 中合格元素的总数。

**示例 1：**

**输入：** nums = [3,1,2], k = 1

**输出：** 2

**解释：**
元素 1 和 2 均有至少 `k = 1` 个元素大于它们。  
没有元素比 3 更大。因此答案是 2。

**示例 2：**

**输入：** nums = [5,5,5], k = 2

**输出：** 0

**解释：**
由于所有元素都等于 5，没有任何元素比其他元素大。因此答案是 0。

**提示：**
  * `1 <= n == nums.length <= 105`
  * `1 <= nums[i] <= 109`
  * `0 <= k < n`

## 示例

```
[3,1,2]
1
[5,5,5]
2
```

## 参考答案

```java
class Solution {
    public int countElements(int[] nums, int k) {
        
    }
}
```

## 一刷思路

```java
class Solution {
    public int countElements(int[] nums, int k) {
        
    }
}
```

## 二刷思路

```java
class Solution {
    public int countElements(int[] nums, int k) {
        
    }
}
```
