# 67. 二进制求和

<table>
<tr><td><b>难度</b></td><td>Easy</td></tr>
<tr><td><b>标签</b></td><td>位运算;数学;字符串;模拟</td></tr>
<tr><td><b>会员</b></td><td>否</td></tr>
<tr><td><b>链接</b></td><td><a href="https://leetcode.cn/problems/add-binary/description/">在线练习</a></td></tr>
</table>

## 题目描述

给你两个二进制字符串 `a` 和 `b` ，以二进制字符串的形式返回它们的和。

**示例  1：**

**输入:** a = "11", b = "1"

**输出：** "100"

**示例  2：**

**输入：** a = "1010", b = "1011"

**输出：** "10101"

**提示：**
  * `1 <= a.length, b.length <= 104`
  * `a` 和 `b` 仅由字符 `'0'` 或 `'1'` 组成
  * 字符串如果不是 `"0"` ，就不含前导零

## 示例

```
"11"
"1"
"1010"
"1011"
```

## 参考答案

```java
class Solution {
    public String addBinary(String a, String b) {
        
    }
}
```

## 一刷思路

```java
class Solution {
    public String addBinary(String a, String b) {
        
    }
}
```

## 二刷思路

```java
class Solution {
    public String addBinary(String a, String b) {
        
    }
}
```
