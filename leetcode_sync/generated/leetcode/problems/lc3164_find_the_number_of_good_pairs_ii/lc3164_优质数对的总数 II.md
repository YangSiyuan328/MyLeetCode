# 3164. 优质数对的总数 II

<table>
<tr><td><b>难度</b></td><td>Medium</td></tr>
<tr><td><b>标签</b></td><td>数组;哈希表</td></tr>
<tr><td><b>会员</b></td><td>否</td></tr>
<tr><td><b>链接</b></td><td><a href="https://leetcode.cn/problems/find-the-number-of-good-pairs-ii/description/">在线练习</a></td></tr>
</table>

## 题目描述

给你两个整数数组 `nums1` 和 `nums2`，长度分别为 `n` 和 `m`。同时给你一个**正整数** `k`。
如果 `nums1[i]` 可以被 `nums2[j] * k` 整除，则称数对 `(i, j)` 为 **优质数对** （`0 <= i <= n - 1`, `0 <= j <= m - 1`）。
返回**优质数对** 的总数。

**示例 1：**

**输入：** nums1 = [1,3,4], nums2 = [1,3,4], k = 1

**输出：** 5

**解释：**
5个优质数对分别是 `(0, 0)`, `(1, 0)`, `(1, 1)`, `(2, 0)`, 和 `(2, 2)`。

**示例 2：**

**输入：** nums1 = [1,2,4,12], nums2 = [2,4], k = 3

**输出：** 2

**解释：**
2个优质数对分别是 `(3, 0)` 和 `(3, 1)`。

**提示：**
  * `1 <= n, m <= 105`
  * `1 <= nums1[i], nums2[j] <= 106`
  * `1 <= k <= 103`

## 示例

```
[1,3,4]
[1,3,4]
1
[1,2,4,12]
[2,4]
3
```

## 参考答案

```java
class Solution {
    public long numberOfPairs(int[] nums1, int[] nums2, int k) {
        
    }
}
```

## 一刷思路

```java
class Solution {
    public long numberOfPairs(int[] nums1, int[] nums2, int k) {
        
    }
}
```

## 二刷思路

```java
class Solution {
    public long numberOfPairs(int[] nums1, int[] nums2, int k) {
        
    }
}
```
