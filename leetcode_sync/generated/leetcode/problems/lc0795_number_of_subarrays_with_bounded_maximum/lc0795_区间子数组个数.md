# 795. 区间子数组个数

<table>
<tr><td><b>难度</b></td><td>Medium</td></tr>
<tr><td><b>标签</b></td><td>数组;双指针</td></tr>
<tr><td><b>会员</b></td><td>否</td></tr>
<tr><td><b>链接</b></td><td><a href="https://leetcode.cn/problems/number-of-subarrays-with-bounded-maximum/description/">在线练习</a></td></tr>
</table>

## 题目描述

给你一个整数数组 `nums` 和两个整数：`left` 及 `right` 。找出 `nums` 中连续、非空且其中最大元素在范围 `[left, right]` 内的子数组，并返回满足条件的子数组的个数。
生成的测试用例保证结果符合 **32-bit** 整数范围。

**示例 1：**

**输入：** nums = [2,1,4,3], left = 2, right = 3

**输出：** 3

**解释：** 满足条件的三个子数组：[2], [2, 1], [3]

**示例 2：**

**输入：** nums = [2,9,2,5,6], left = 2, right = 8

**输出：** 7

**提示：**
  * `1 <= nums.length <= 105`
  * `0 <= nums[i] <= 109`
  * `0 <= left <= right <= 109`

## 示例

```
[2,1,4,3]
2
3
[2,9,2,5,6]
2
8
```

## 参考答案

```java
class Solution {
    public int numSubarrayBoundedMax(int[] nums, int left, int right) {
        
    }
}
```

## 一刷思路

```java
class Solution {
    public int numSubarrayBoundedMax(int[] nums, int left, int right) {
        
    }
}
```

## 二刷思路

```java
class Solution {
    public int numSubarrayBoundedMax(int[] nums, int left, int right) {
        
    }
}
```
