# 1550. 存在连续三个奇数的数组

<table>
<tr><td><b>难度</b></td><td>Easy</td></tr>
<tr><td><b>标签</b></td><td>数组</td></tr>
<tr><td><b>会员</b></td><td>否</td></tr>
<tr><td><b>链接</b></td><td><a href="https://leetcode.cn/problems/three-consecutive-odds/description/">在线练习</a></td></tr>
</table>

## 题目描述

给你一个整数数组 `arr`，请你判断数组中是否存在连续三个元素都是奇数的情况：如果存在，请返回 `true` ；否则，返回 `false` 。

**示例 1：**

**输入：** arr = [2,6,4,1]

**输出：** false

**解释：** 不存在连续三个元素都是奇数的情况。

**示例 2：**

**输入：** arr = [1,2,34,3,4,5,7,23,12]

**输出：** true

**解释：** 存在连续三个元素都是奇数的情况，即 [5,7,23] 。

**提示：**
  * `1 <= arr.length <= 1000`
  * `1 <= arr[i] <= 1000`

## 示例

```
[2,6,4,1]
[1,2,34,3,4,5,7,23,12]
```

## 参考答案

```java
class Solution {
    public boolean threeConsecutiveOdds(int[] arr) {
        
    }
}
```

## 一刷思路

```java
class Solution {
    public boolean threeConsecutiveOdds(int[] arr) {
        
    }
}
```

## 二刷思路

```java
class Solution {
    public boolean threeConsecutiveOdds(int[] arr) {
        
    }
}
```
