# 1745. 分割回文串 IV

<table>
<tr><td><b>难度</b></td><td>Hard</td></tr>
<tr><td><b>标签</b></td><td>字符串;动态规划</td></tr>
<tr><td><b>会员</b></td><td>否</td></tr>
<tr><td><b>链接</b></td><td><a href="https://leetcode.cn/problems/palindrome-partitioning-iv/description/">在线练习</a></td></tr>
</table>

## 题目描述

给你一个字符串 `s` ，如果可以将它分割成三个 **非空** 回文子字符串，那么返回 `true` ，否则返回 `false` 。
当一个字符串正着读和反着读是一模一样的，就称其为 **回文字符串** 。

**示例 1：**

**输入：** s = "abcbdd"

**输出：** true

**解释：** "abcbdd" = "a" + "bcb" + "dd"，三个子字符串都是回文的。

**示例 2：**

**输入：** s = "bcbddxy"

**输出：** false

**解释：** s 没办法被分割成 3 个回文子字符串。

**提示：**
  * `3 <= s.length <= 2000`
  * `s`​​​​​​ 只包含小写英文字母。

## 示例

```
"abcbdd"
"bcbddxy"
```

## 参考答案

```java
class Solution {
    public boolean checkPartitioning(String s) {
        
    }
}
```

## 一刷思路

```java
class Solution {
    public boolean checkPartitioning(String s) {
        
    }
}
```

## 二刷思路

```java
class Solution {
    public boolean checkPartitioning(String s) {
        
    }
}
```
