# 1787. 使所有区间的异或结果为零

<table>
<tr><td><b>难度</b></td><td>Hard</td></tr>
<tr><td><b>标签</b></td><td>位运算;数组;哈希表;动态规划;计数</td></tr>
<tr><td><b>会员</b></td><td>否</td></tr>
<tr><td><b>链接</b></td><td><a href="https://leetcode.cn/problems/make-the-xor-of-all-segments-equal-to-zero/description/">在线练习</a></td></tr>
</table>

## 题目描述

给你一个整数数组 `nums`​​​ 和一个整数 `k`​​​​​ 。区间 `[left, right]`（`left <= right`）的 **异或结果** 是对下标位于 `left` 和 `right`（包括 `left` 和 `right` ）之间所有元素进行 `XOR` 运算的结果：`nums[left] XOR nums[left+1] XOR ... XOR nums[right]` 。
返回数组中 **要更改的最小元素数** ，以使所有长度为 `k` 的区间异或结果等于零。

**示例 1：**

**输入：** nums = [1,2,0,3,0], k = 1

**输出：** 3

**解释：** 将数组 [**1** ,**2** ,0,**3** ,0] 修改为 [**0** ,**0** ,0,**0** ,0]

**示例 2：**

**输入：** nums = [3,4,5,2,1,7,3,4,7], k = 3

**输出：** 3

**解释：** 将数组 [3,4,**5** ,**2** ,**1** ,7,3,4,7] 修改为 [3,4,**7** ,**3** ,**4** ,7,3,4,7]

**示例 3：**

**输入：** nums = [1,2,4,1,2,5,1,2,6], k = 3

**输出：** 3

**解释：** 将数组[1,2,**4,** 1,2,**5** ,1,2,**6**] 修改为 [1,2,**3** ,1,2,**3** ,1,2,**3**]

**提示：**
  * `1 <= k <= nums.length <= 2000`
  * `​​​​​​0 <= nums[i] < 210`

## 示例

```
[1,2,0,3,0]
1
[3,4,5,2,1,7,3,4,7]
3
[1,2,4,1,2,5,1,2,6]
3
```

## 参考答案

```java
class Solution {
    public int minChanges(int[] nums, int k) {
        
    }
}
```

## 一刷思路

```java
class Solution {
    public int minChanges(int[] nums, int k) {
        
    }
}
```

## 二刷思路

```java
class Solution {
    public int minChanges(int[] nums, int k) {
        
    }
}
```
