# 932. 漂亮数组

<table>
<tr><td><b>难度</b></td><td>Medium</td></tr>
<tr><td><b>标签</b></td><td>数组;数学;分治</td></tr>
<tr><td><b>会员</b></td><td>否</td></tr>
<tr><td><b>链接</b></td><td><a href="https://leetcode.cn/problems/beautiful-array/description/">在线练习</a></td></tr>
</table>

## 题目描述

如果长度为 `n` 的数组 `nums` 满足下述条件，则认为该数组是一个 **漂亮数组** ：
  * `nums` 是由范围 `[1, n]` 的整数组成的一个排列。
  * 对于每个 `0 <= i < j < n` ，均不存在下标 `k`（`i < k < j`）使得 `2 * nums[k] == nums[i] + nums[j]` 。
给你整数 `n` ，返回长度为 `n` 的任一 **漂亮数组** 。本题保证对于给定的 `n` 至少存在一个有效答案。

**示例 1 ：**

**输入：** n = 4

**输出：**[2,1,4,3]

**示例 2 ：**

**输入：** n = 5

**输出：**[3,1,2,5,4]

**提示：**
  * `1 <= n <= 1000`

## 示例

```
4
5
```

## 参考答案

```java
class Solution {
    public int[] beautifulArray(int n) {
        
    }
}
```

## 一刷思路

```java
class Solution {
    public int[] beautifulArray(int n) {
        
    }
}
```

## 二刷思路

```java
class Solution {
    public int[] beautifulArray(int n) {
        
    }
}
```
