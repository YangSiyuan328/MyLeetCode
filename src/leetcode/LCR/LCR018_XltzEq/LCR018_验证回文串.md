# LCR 018. 验证回文串

<table>
<tr><td><b>难度</b></td><td>Easy</td></tr>
<tr><td><b>标签</b></td><td>双指针;字符串</td></tr>
<tr><td><b>会员</b></td><td>否</td></tr>
<tr><td><b>链接</b></td><td><a href="https://leetcode.cn/problems/XltzEq/description/">在线练习</a></td></tr>
</table>

## 题目描述

给定一个字符串 `s` ，验证 `s` 是否是 **回文串  **，只考虑字母和数字字符，可以忽略字母的大小写。
本题中，将空字符串定义为有效的 **回文串  **。

**示例 1：**

**输入:** s =**** "A man, a plan, a canal: Panama"

**输出:** true

**解释：** "amanaplanacanalpanama" 是回文串

**示例 2：**

**输入:** s = "race a car"

**输出:** false

**解释：** "raceacar" 不是回文串

**提示：**
  * `1 <= s.length <= 2 * 105`
  * 字符串 `s` 由 ASCII 字符组成
注意：本题与主站 125 题相同： <https://leetcode.cn/problems/valid-palindrome/>

## 示例

```
"A man, a plan, a canal: Panama"
"race a car"
```

## 参考答案

```java
class Solution {
    public boolean isPalindrome(String s) {

    }
}
```

## 一刷思路

```java
class Solution {
    public boolean isPalindrome(String s) {

    }
}
```

## 二刷思路

```java
class Solution {
    public boolean isPalindrome(String s) {

    }
}
```
