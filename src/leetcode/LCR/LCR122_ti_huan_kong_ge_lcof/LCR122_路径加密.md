# LCR 122. 路径加密

<table>
<tr><td><b>难度</b></td><td>Easy</td></tr>
<tr><td><b>标签</b></td><td>字符串</td></tr>
<tr><td><b>会员</b></td><td>否</td></tr>
<tr><td><b>链接</b></td><td><a href="https://leetcode.cn/problems/ti-huan-kong-ge-lcof/description/">在线练习</a></td></tr>
</table>

## 题目描述

假定一段路径记作字符串 `path`，其中以 "`.`" 作为分隔符。现需将路径加密，加密方法为将 `path` 中的分隔符替换为空格 "` `"，请返回加密后的字符串。

**示例 1：**

**输入：** path = "a.aef.qerf.bb"

**输出：** "a aef qerf bb"

**限制：**
`0 <= path.length <= 10000`

## 示例

```
"a.aef.qerf.bb"
```

## 参考答案

```java
class Solution {
    public String pathEncryption(String path) {
        
    }
}
```

## 一刷思路

```java
class Solution {
    public String pathEncryption(String path) {
        
    }
}
```

## 二刷思路

```java
class Solution {
    public String pathEncryption(String path) {
        
    }
}
```
