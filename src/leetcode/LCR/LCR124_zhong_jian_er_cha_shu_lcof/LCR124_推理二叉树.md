# LCR 124. 推理二叉树

<table>
<tr><td><b>难度</b></td><td>Medium</td></tr>
<tr><td><b>标签</b></td><td>树;数组;哈希表;分治;二叉树</td></tr>
<tr><td><b>会员</b></td><td>否</td></tr>
<tr><td><b>链接</b></td><td><a href="https://leetcode.cn/problems/zhong-jian-er-cha-shu-lcof/description/">在线练习</a></td></tr>
</table>

## 题目描述

某二叉树的先序遍历结果记录于整数数组 `preorder`，它的中序遍历结果记录于整数数组 `inorder`。请根据 `preorder` 和 `inorder` 的提示构造出这棵二叉树并返回其根节点。
注意：`preorder` 和 `inorder` 中均不含重复数字。

**示例 1：**

![](https://assets.leetcode.com/uploads/2021/02/19/tree.jpg)

**输入:** preorder = [3,9,20,15,7], inorder = [9,3,15,20,7]

**输出:**[3,9,20,null,null,15,7]

**示例 2:**

**输入:** preorder = [-1], inorder = [-1]

**输出:** [-1]

**提示:**
  * `1 <= preorder.length <= 3000`
  * `inorder.length == preorder.length`
  * `-3000 <= preorder[i], inorder[i] <= 3000`
  * `inorder` 均出现在 `preorder`
  * `preorder` 保证 为二叉树的前序遍历序列
  * `inorder` 保证 为二叉树的中序遍历序列
注意：本题与主站 105 题重复：<https://leetcode.cn/problems/construct-binary-tree-from-preorder-and-inorder-traversal/>

## 示例

```
[3,9,20,15,7]
[9,3,15,20,7]
```

## 参考答案

```java
class Solution {
    public TreeNode deduceTree(int[] preorder, int[] inorder) {
        
    }
}
```

## 一刷思路

```java
class Solution {
    public TreeNode deduceTree(int[] preorder, int[] inorder) {
        
    }
}
```

## 二刷思路

```java
class Solution {
    public TreeNode deduceTree(int[] preorder, int[] inorder) {
        
    }
}
```
