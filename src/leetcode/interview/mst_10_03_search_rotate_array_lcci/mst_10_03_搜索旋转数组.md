# 面试题 10.03. 搜索旋转数组

<table>
<tr><td><b>难度</b></td><td>Medium</td></tr>
<tr><td><b>标签</b></td><td>数组;二分查找</td></tr>
<tr><td><b>会员</b></td><td>否</td></tr>
<tr><td><b>链接</b></td><td><a href="https://leetcode.cn/problems/search-rotate-array-lcci/description/">在线练习</a></td></tr>
</table>

## 题目描述

搜索旋转数组。给定一个排序后的数组，包含n个整数，但这个数组已被旋转过很多次了，次数不详。请编写代码找出数组中的某个元素，假设数组元素原先是按升序排列的。若有多个相同元素，返回索引值最小的一个。

**示例 1：**

**输入：** arr = [15, 16, 19, 20, 25, 1, 3, 4, 5, 7, 10, 14], target = 5

**输出：** 8（元素5在该数组中的索引）

**示例 2：**

**输入** ：arr = [15, 16, 19, 20, 25, 1, 3, 4, 5, 7, 10, 14], target = 11

**输出** ：-1 （没有找到）

**提示:**
  1. arr 长度范围在[1, 1000000]之间

## 示例

```
[15, 16, 19, 20, 25, 1, 3, 4, 5, 7, 10, 14]
5
[1,1,1,1,1,2,1,1,1]
2
```

## 参考答案

```java
class Solution {
    public int search(int[] arr, int target) {
        
    }
}
```

## 一刷思路

```java
class Solution {
    public int search(int[] arr, int target) {
        
    }
}
```

## 二刷思路

```java
class Solution {
    public int search(int[] arr, int target) {
        
    }
}
```
