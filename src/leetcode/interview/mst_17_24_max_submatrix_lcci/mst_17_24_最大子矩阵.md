# 面试题 17.24. 最大子矩阵

<table>
<tr><td><b>难度</b></td><td>Hard</td></tr>
<tr><td><b>标签</b></td><td>数组;动态规划;矩阵;前缀和</td></tr>
<tr><td><b>会员</b></td><td>否</td></tr>
<tr><td><b>链接</b></td><td><a href="https://leetcode.cn/problems/max-submatrix-lcci/description/">在线练习</a></td></tr>
</table>

## 题目描述

给定一个正整数、负整数和 0 组成的 N × M 矩阵，编写代码找出元素总和最大的子矩阵。
返回一个数组 `[r1, c1, r2, c2]`，其中 `r1`, `c1` 分别代表子矩阵左上角的行号和列号，`r2`, `c2` 分别代表右下角的行号和列号。若有多个满足条件的子矩阵，返回任意一个均可。

**注意：** 本题相对书上原题稍作改动

**示例：**

**输入：**[
   [-1,**0**],
   [0,-1]
]

**输出：**[0,1,0,1]

**解释：** 输入中标粗的元素即为输出所表示的矩阵

**说明：**
  * `1 <= matrix.length, matrix[0].length <= 200`

## 示例

```
[[9,-8,1,3,-2],[-3,7,6,-2,4],[6,-4,-4,8,-7]]
```

## 参考答案

```java
class Solution {
    public int[] getMaxMatrix(int[][] matrix) {
        
    }
}
```

## 一刷思路

```java
class Solution {
    public int[] getMaxMatrix(int[][] matrix) {
        
    }
}
```

## 二刷思路

```java
class Solution {
    public int[] getMaxMatrix(int[][] matrix) {
        
    }
}
```
