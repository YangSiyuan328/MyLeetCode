# 961. 在长度 2N 的数组中找出重复 N 次的元素

<table>
<tr><td><b>难度</b></td><td>Easy</td></tr>
<tr><td><b>标签</b></td><td>数组;哈希表</td></tr>
<tr><td><b>会员</b></td><td>否</td></tr>
<tr><td><b>链接</b></td><td><a href="https://leetcode.cn/problems/n-repeated-element-in-size-2n-array/description/">在线练习</a></td></tr>
</table>

## 题目描述

给你一个整数数组 `nums` ，该数组具有以下属性：
  * `nums.length == 2 * n`.
  * `nums` 包含 `n + 1` 个 **不同的** 元素，其中 `n` 个值在数组中出现 **恰好一次** 。
  * `nums` 中恰有一个元素重复 `n` 次
找出并返回重复了 `n` __ 次的那个元素。

**示例 1：**

**输入：** nums = [1,2,3,3]

**输出：** 3

**示例 2：**

**输入：** nums = [2,1,2,5,3,2]

**输出：** 2

**示例 3：**

**输入：** nums = [5,1,5,2,5,3,5,4]

**输出：** 5

**提示：**
  * `2 <= n <= 5000`
  * `nums.length == 2 * n`
  * `0 <= nums[i] <= 104`
  * `nums` 由 `n + 1` 个**不同的** 元素组成，且其中一个元素恰好重复 `n` 次

## 示例

```
[1,2,3,3]
[2,1,2,5,3,2]
[5,1,5,2,5,3,5,4]
```

## 参考答案

```java
class Solution {
    public int repeatedNTimes(int[] nums) {
        
    }
}
```

## 一刷思路

```java
class Solution {
    public int repeatedNTimes(int[] nums) {
        
    }
}
```

## 二刷思路

```java
class Solution {
    public int repeatedNTimes(int[] nums) {
        
    }
}
```
