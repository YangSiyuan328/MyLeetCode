# 2443. 反转之后的数字和

<table>
<tr><td><b>难度</b></td><td>Medium</td></tr>
<tr><td><b>标签</b></td><td>数学;枚举</td></tr>
<tr><td><b>会员</b></td><td>否</td></tr>
<tr><td><b>链接</b></td><td><a href="https://leetcode.cn/problems/sum-of-number-and-its-reverse/description/">在线练习</a></td></tr>
</table>

## 题目描述

给你一个 **非负** 整数 `num` 。如果存在某个 **非负** 整数 `k` 满足 `k + reverse(k) = num`  ，则返回 `true` ；否则，返回 __`false` 。
`reverse(k)` 表示 `k` 反转每个数位后得到的数字。

**示例 1：**

**输入：** num = 443

**输出：** true

**解释：** 172 + 271 = 443 ，所以返回 true 。

**示例 2：**

**输入：** num = 63

**输出：** false

**解释：** 63 不能表示为非负整数及其反转后数字之和，返回 false 。

**示例 3：**

**输入：** num = 181

**输出：** true

**解释：** 140 + 041 = 181 ，所以返回 true 。注意，反转后的数字可能包含前导零。

**提示：**
  * `0 <= num <= 105`

## 示例

```
443
63
181
```

## 参考答案

```java
class Solution {
    public boolean sumOfNumberAndReverse(int num) {
        
    }
}
```

## 一刷思路

```java
class Solution {
    public boolean sumOfNumberAndReverse(int num) {
        
    }
}
```

## 二刷思路

```java
class Solution {
    public boolean sumOfNumberAndReverse(int num) {
        
    }
}
```
