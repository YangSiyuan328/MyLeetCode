# 14. 最长公共前缀

<table>
<tr><td><b>难度</b></td><td>Easy</td></tr>
<tr><td><b>标签</b></td><td>字典树;数组;字符串</td></tr>
<tr><td><b>会员</b></td><td>否</td></tr>
<tr><td><b>链接</b></td><td><a href="https://leetcode.cn/problems/longest-common-prefix/description/">在线练习</a></td></tr>
</table>

## 题目描述

编写一个函数来查找字符串数组中的最长公共前缀。
如果不存在公共前缀，返回空字符串 `""`。

**示例 1：**

**输入：** strs = ["flower","flow","flight"]

**输出：** "fl"

**示例 2：**

**输入：** strs = ["dog","racecar","car"]

**输出：** ""

**解释：** 输入不存在公共前缀。

**提示：**
  * `1 <= strs.length <= 200`
  * `0 <= strs[i].length <= 200`
  * `strs[i]` 如果非空，则仅由小写英文字母组成

## 示例

```
["flower","flow","flight"]
["dog","racecar","car"]
```

## 参考答案

```java
class Solution {
    public String longestCommonPrefix(String[] strs) {
        
    }
}
```

## 一刷思路

```java
class Solution {
    public String longestCommonPrefix(String[] strs) {
        
    }
}
```

## 二刷思路

```java
class Solution {
    public String longestCommonPrefix(String[] strs) {
        
    }
}
```
