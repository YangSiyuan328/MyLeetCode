# 845. 数组中的最长山脉

<table>
<tr><td><b>难度</b></td><td>Medium</td></tr>
<tr><td><b>标签</b></td><td>数组;双指针;动态规划;枚举</td></tr>
<tr><td><b>会员</b></td><td>否</td></tr>
<tr><td><b>链接</b></td><td><a href="https://leetcode.cn/problems/longest-mountain-in-array/description/">在线练习</a></td></tr>
</table>

## 题目描述

把符合下列属性的数组 `arr` 称为 **山脉数组** ：
  * `arr.length >= 3`
  * 存在下标 `i`（`0 < i < arr.length - 1`），满足 
* `arr[0] < arr[1] < ... < arr[i - 1] < arr[i]`
* `arr[i] > arr[i + 1] > ... > arr[arr.length - 1]`
给出一个整数数组 `arr`，返回最长山脉子数组的长度。如果不存在山脉子数组，返回 `0` 。

**示例 1：**

**输入：** arr = [2,1,4,7,3,2,5]

**输出：** 5

**解释：** 最长的山脉子数组是 [1,4,7,3,2]，长度为 5。

**示例 2：**

**输入：** arr = [2,2,2]

**输出：** 0

**解释：** 不存在山脉子数组。

**提示：**
  * `1 <= arr.length <= 104`
  * `0 <= arr[i] <= 104`

**进阶：**
  * 你可以仅用一趟扫描解决此问题吗？
  * 你可以用 `O(1)` 空间解决此问题吗？

## 示例

```
[2,1,4,7,3,2,5]
[2,2,2]
```

## 参考答案

```java
class Solution {
    public int longestMountain(int[] arr) {
        
    }
}
```

## 一刷思路

```java
class Solution {
    public int longestMountain(int[] arr) {
        
    }
}
```

## 二刷思路

```java
class Solution {
    public int longestMountain(int[] arr) {
        
    }
}
```
