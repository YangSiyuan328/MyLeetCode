# 905. 按奇偶排序数组

<table>
<tr><td><b>难度</b></td><td>Easy</td></tr>
<tr><td><b>标签</b></td><td>数组;双指针;排序</td></tr>
<tr><td><b>会员</b></td><td>否</td></tr>
<tr><td><b>链接</b></td><td><a href="https://leetcode.cn/problems/sort-array-by-parity/description/">在线练习</a></td></tr>
</table>

## 题目描述

给你一个整数数组 `nums`，将 `nums` 中的的所有偶数元素移动到数组的前面，后跟所有奇数元素。
返回满足此条件的 **任一数组** 作为答案。

**示例 1：**

**输入：** nums = [3,1,2,4]

**输出：**[2,4,3,1]

**解释：**[4,2,3,1]、[2,4,1,3] 和 [4,2,1,3] 也会被视作正确答案。

**示例 2：**

**输入：** nums = [0]

**输出：**[0]

**提示：**
  * `1 <= nums.length <= 5000`
  * `0 <= nums[i] <= 5000`

## 示例

```
[3,1,2,4]
[0]
```

## 参考答案

```java
class Solution {
    public int[] sortArrayByParity(int[] nums) {
        
    }
}
```

## 一刷思路

```java
class Solution {
    public int[] sortArrayByParity(int[] nums) {
        
    }
}
```

## 二刷思路

```java
class Solution {
    public int[] sortArrayByParity(int[] nums) {
        
    }
}
```
