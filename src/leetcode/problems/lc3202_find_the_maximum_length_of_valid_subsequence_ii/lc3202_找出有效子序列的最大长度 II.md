# 3202. 找出有效子序列的最大长度 II

<table>
<tr><td><b>难度</b></td><td>Medium</td></tr>
<tr><td><b>标签</b></td><td>数组;动态规划</td></tr>
<tr><td><b>会员</b></td><td>否</td></tr>
<tr><td><b>链接</b></td><td><a href="https://leetcode.cn/problems/find-the-maximum-length-of-valid-subsequence-ii/description/">在线练习</a></td></tr>
</table>

## 题目描述

给你一个整数数组 `nums` 和一个 **正**  整数 `k` 。 
`nums` 的一个 子序列 `sub` 的长度为 `x` ，如果其满足以下条件，则称其为 **有效子序列**  ：
  * `(sub[0] + sub[1]) % k == (sub[1] + sub[2]) % k == ... == (sub[x - 2] + sub[x - 1]) % k`
返回 `nums` 的 **最长****有效子序列**  的长度。 

**示例 1：**

**输入：** nums = [1,2,3,4,5], k = 2

**输出：** 5

**解释：**
最长有效子序列是 `[1, 2, 3, 4, 5]` 。

**示例 2：**

**输入：** nums = [1,4,2,3,1,4], k = 3

**输出：** 4

**解释：**
最长有效子序列是 `[1, 4, 1, 4]` 。

**提示：**
  * `2 <= nums.length <= 103`
  * `1 <= nums[i] <= 107`
  * `1 <= k <= 103`

## 示例

```
[1,2,3,4,5]
2
[1,4,2,3,1,4]
3
```

## 参考答案

```java
class Solution {
    public int maximumLength(int[] nums, int k) {
        
    }
}
```

## 一刷思路

```java
class Solution {
    public int maximumLength(int[] nums, int k) {
        
    }
}
```

## 二刷思路

```java
class Solution {
    public int maximumLength(int[] nums, int k) {
        
    }
}
```
