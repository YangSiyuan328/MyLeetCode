# 2636. Promise 对象池

<table>
<tr><td><b>难度</b></td><td>Medium</td></tr>
<tr><td><b>标签</b></td><td></td></tr>
<tr><td><b>会员</b></td><td>是</td></tr>
<tr><td><b>链接</b></td><td><a href="https://leetcode.cn/problems/promise-pool/description/">在线练习</a></td></tr>
</table>

## 题目描述



## 示例

```
[() => new Promise(res => setTimeout(res, 300)), () => new Promise(res => setTimeout(res, 400)), () => new Promise(res => setTimeout(res, 200))]
2
[() => new Promise(res => setTimeout(res, 300)), () => new Promise(res => setTimeout(res, 400)), () => new Promise(res => setTimeout(res, 200))]
5
[() => new Promise(res => setTimeout(res, 300)), () => new Promise(res => setTimeout(res, 400)), () => new Promise(res => setTimeout(res, 200))]
1
```

## 参考答案

```java

```

## 一刷思路

```java

```

## 二刷思路

```java

```
