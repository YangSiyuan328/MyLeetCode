# 537. 复数乘法

<table>
<tr><td><b>难度</b></td><td>Medium</td></tr>
<tr><td><b>标签</b></td><td>数学;字符串;模拟</td></tr>
<tr><td><b>会员</b></td><td>否</td></tr>
<tr><td><b>链接</b></td><td><a href="https://leetcode.cn/problems/complex-number-multiplication/description/">在线练习</a></td></tr>
</table>

## 题目描述

[复数](https://baike.baidu.com/item/%E5%A4%8D%E6%95%B0/254365?fr=aladdin) 可以用字符串表示，遵循 `"**实部** +**虚部** i"` 的形式，并满足下述条件：
  * `实部` 是一个整数，取值范围是 `[-100, 100]`
  * `虚部` 也是一个整数，取值范围是 `[-100, 100]`
  * `i2 == -1`
给你两个字符串表示的复数 `num1` 和 `num2` ，请你遵循复数表示形式，返回表示它们乘积的字符串。

**示例 1：**

**输入：** num1 = "1+1i", num2 = "1+1i"

**输出：** "0+2i"

**解释：**(1 + i) * (1 + i) = 1 + i2 + 2 * i = 2i ，你需要将它转换为 0+2i 的形式。

**示例 2：**

**输入：** num1 = "1+-1i", num2 = "1+-1i"

**输出：** "0+-2i"

**解释：**(1 - i) * (1 - i) = 1 + i2 - 2 * i = -2i ，你需要将它转换为 0+-2i 的形式。 

**提示：**
  * `num1` 和 `num2` 都是有效的复数表示。

## 示例

```
"1+1i"
"1+1i"
"1+-1i"
"1+-1i"
```

## 参考答案

```java
class Solution {
    public String complexNumberMultiply(String num1, String num2) {
        
    }
}
```

## 一刷思路

```java
class Solution {
    public String complexNumberMultiply(String num1, String num2) {
        
    }
}
```

## 二刷思路

```java
class Solution {
    public String complexNumberMultiply(String num1, String num2) {
        
    }
}
```
