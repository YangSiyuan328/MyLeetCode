# 1002. 查找共用字符

<table>
<tr><td><b>难度</b></td><td>Easy</td></tr>
<tr><td><b>标签</b></td><td>数组;哈希表;字符串</td></tr>
<tr><td><b>会员</b></td><td>否</td></tr>
<tr><td><b>链接</b></td><td><a href="https://leetcode.cn/problems/find-common-characters/description/">在线练习</a></td></tr>
</table>

## 题目描述

给你一个字符串数组 `words` ，请你找出所有在 `words` 的每个字符串中都出现的共用字符（**包括重复字符** ），并以数组形式返回。你可以按 **任意顺序** 返回答案。 

**示例 1：**

**输入：** words = ["bella","label","roller"]

**输出：**["e","l","l"]

**示例 2：**

**输入：** words = ["cool","lock","cook"]

**输出：**["c","o"]

**提示：**
  * `1 <= words.length <= 100`
  * `1 <= words[i].length <= 100`
  * `words[i]` 由小写英文字母组成

## 示例

```
["bella","label","roller"]
["cool","lock","cook"]
```

## 参考答案

```java
class Solution {
    public List<String> commonChars(String[] words) {
        
    }
}
```

## 一刷思路

```java
class Solution {
    public List<String> commonChars(String[] words) {
        
    }
}
```

## 二刷思路

```java
class Solution {
    public List<String> commonChars(String[] words) {
        
    }
}
```
