# 3212. 统计 X 和 Y 频数相等的子矩阵数量

<table>
<tr><td><b>难度</b></td><td>Medium</td></tr>
<tr><td><b>标签</b></td><td>数组;矩阵;前缀和</td></tr>
<tr><td><b>会员</b></td><td>否</td></tr>
<tr><td><b>链接</b></td><td><a href="https://leetcode.cn/problems/count-submatrices-with-equal-frequency-of-x-and-y/description/">在线练习</a></td></tr>
</table>

## 题目描述

给你一个二维字符矩阵 `grid`，其中 `grid[i][j]` 可能是 `'X'`、`'Y'` 或 `'.'`，返回满足以下条件的子矩阵数量：
  * 包含 `grid[0][0]`
  * `'X'` 和 `'Y'` 的频数相等。
  * 至少包含一个 `'X'`。

**示例 1：**

**输入：** grid = [["X","Y","."],["Y",".","."]]

**输出：** 3

**解释：**

**![](https://assets.leetcode.com/uploads/2024/06/07/examplems.png)**

**示例 2：**

**输入：** grid = [["X","X"],["X","Y"]]

**输出：** 0

**解释：**
不存在满足 `'X'` 和 `'Y'` 频数相等的子矩阵。

**示例 3：**

**输入：** grid = [[".","."],[".","."]]

**输出：** 0

**解释：**
不存在满足至少包含一个 `'X'` 的子矩阵。

**提示：**
  * `1 <= grid.length, grid[i].length <= 1000`
  * `grid[i][j]` 可能是 `'X'`、`'Y'` 或 `'.'`.

## 示例

```
[["X","Y","."],["Y",".","."]]
[["X","X"],["X","Y"]]
[[".","."],[".","."]]
```

## 参考答案

```java
class Solution {
    public int numberOfSubmatrices(char[][] grid) {
        
    }
}
```

## 一刷思路

```java
class Solution {
    public int numberOfSubmatrices(char[][] grid) {
        
    }
}
```

## 二刷思路

```java
class Solution {
    public int numberOfSubmatrices(char[][] grid) {
        
    }
}
```
