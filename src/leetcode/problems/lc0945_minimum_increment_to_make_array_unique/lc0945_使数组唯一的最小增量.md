# 945. 使数组唯一的最小增量

<table>
<tr><td><b>难度</b></td><td>Medium</td></tr>
<tr><td><b>标签</b></td><td>贪心;数组;计数;排序</td></tr>
<tr><td><b>会员</b></td><td>否</td></tr>
<tr><td><b>链接</b></td><td><a href="https://leetcode.cn/problems/minimum-increment-to-make-array-unique/description/">在线练习</a></td></tr>
</table>

## 题目描述

给你一个整数数组 `nums` 。每次 move 操作将会选择任意一个满足 `0 <= i < nums.length` 的下标 `i`，并将 `nums[i]` 递增 `1`。
返回使 `nums` 中的每个值都变成唯一的所需要的最少操作次数。
生成的测试用例保证答案在 32 位整数范围内。

**示例 1：**

**输入：** nums = [1,2,2]

**输出：** 1

**解释：** 经过一次 _move_ 操作，数组将变为 [1, 2, 3]。

**示例 2：**

**输入：** nums = [3,2,1,2,1,7]

**输出：** 6

**解释：** 经过 6 次 _move_ 操作，数组将变为 [3, 4, 1, 2, 5, 7]。
可以看出 5 次或 5 次以下的 _move_ 操作是不能让数组的每个值唯一的。

**提示：**
  * `1 <= nums.length <= 105`
  * `0 <= nums[i] <= 105`

## 示例

```
[1,2,2]
[3,2,1,2,1,7]
```

## 参考答案

```java
class Solution {
    public int minIncrementForUnique(int[] nums) {
        
    }
}
```

## 一刷思路

```java
class Solution {
    public int minIncrementForUnique(int[] nums) {
        
    }
}
```

## 二刷思路

```java
class Solution {
    public int minIncrementForUnique(int[] nums) {
        
    }
}
```
