# 2667. 创建 Hello World 函数

<table>
<tr><td><b>难度</b></td><td>Easy</td></tr>
<tr><td><b>标签</b></td><td></td></tr>
<tr><td><b>会员</b></td><td>否</td></tr>
<tr><td><b>链接</b></td><td><a href="https://leetcode.cn/problems/create-hello-world-function/description/">在线练习</a></td></tr>
</table>

## 题目描述

请你编写一个名为 `createHelloWorld` 的函数。它应该返回一个新的函数，该函数总是返回 `"Hello World"` 。 

**示例 1：**

**输入：** args = []

**输出：** "Hello World"

**解释：**
const f = createHelloWorld();
f(); // "Hello World"
createHelloWorld 返回的函数应始终返回 "Hello World"。

**示例 2：**

**输入：** args = [{},null,42]

**输出：** "Hello World"

**解释：**
const f = createHelloWorld();
f({}, null, 42); // "Hello World"
可以传递任何参数给函数，但它应始终返回 "Hello World"。

**提示：**
  * `0 <= args.length <= 10`

## 示例

```
[]
[{},null,42]
```

## 参考答案

```java

```

## 一刷思路

```java

```

## 二刷思路

```java

```
