# 301. 删除无效的括号

<table>
<tr><td><b>难度</b></td><td>Hard</td></tr>
<tr><td><b>标签</b></td><td>广度优先搜索;字符串;回溯</td></tr>
<tr><td><b>会员</b></td><td>否</td></tr>
<tr><td><b>链接</b></td><td><a href="https://leetcode.cn/problems/remove-invalid-parentheses/description/">在线练习</a></td></tr>
</table>

## 题目描述

给你一个由若干括号和字母组成的字符串 `s` ，删除最小数量的无效括号，使得输入的字符串有效。
返回所有可能的结果。答案可以按 **任意顺序** 返回。

**示例 1：**

**输入：** s = "()())()"

**输出：**["(())()","()()()"]

**示例 2：**

**输入：** s = "(a)())()"

**输出：**["(a())()","(a)()()"]

**示例 3：**

**输入：** s = ")("

**输出：**[""]

**提示：**
  * `1 <= s.length <= 25`
  * `s` 由小写英文字母以及括号 `'('` 和 `')'` 组成
  * `s` 中至多含 `20` 个括号

## 示例

```
"()())()"
"(a)())()"
")("
```

## 参考答案

```java
class Solution {
    public List<String> removeInvalidParentheses(String s) {
        
    }
}
```

## 一刷思路

```java
class Solution {
    public List<String> removeInvalidParentheses(String s) {
        
    }
}
```

## 二刷思路

```java
class Solution {
    public List<String> removeInvalidParentheses(String s) {
        
    }
}
```
