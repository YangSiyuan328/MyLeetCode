# 316. 去除重复字母

<table>
<tr><td><b>难度</b></td><td>Medium</td></tr>
<tr><td><b>标签</b></td><td>栈;贪心;字符串;单调栈</td></tr>
<tr><td><b>会员</b></td><td>否</td></tr>
<tr><td><b>链接</b></td><td><a href="https://leetcode.cn/problems/remove-duplicate-letters/description/">在线练习</a></td></tr>
</table>

## 题目描述

给你一个字符串 `s` ，请你去除字符串中重复的字母，使得每个字母只出现一次。需保证 **返回结果的 字典序最小**（要求不能打乱其他字符的相对位置）。

**示例 1：**

**输入：**s = "bcabc"

**输出：**"abc"

**示例 2：**

**输入：**s = "cbacdcbc"

**输出：**"acdb"

**提示：**
  * `1 <= s.length <= 104`
  * `s` 由小写英文字母组成

**注意：** 该题与 1081 <https://leetcode.cn/problems/smallest-subsequence-of-distinct-characters> 相同

## 示例

```
"bcabc"
"cbacdcbc"
```

## 参考答案

```java
class Solution {
    public String removeDuplicateLetters(String s) {
        
    }
}
```

## 一刷思路

```java
class Solution {
    public String removeDuplicateLetters(String s) {
        
    }
}
```

## 二刷思路

```java
class Solution {
    public String removeDuplicateLetters(String s) {
        
    }
}
```
