# 2772. 使数组中的所有元素都等于零

<table>
<tr><td><b>难度</b></td><td>Medium</td></tr>
<tr><td><b>标签</b></td><td>数组;前缀和</td></tr>
<tr><td><b>会员</b></td><td>否</td></tr>
<tr><td><b>链接</b></td><td><a href="https://leetcode.cn/problems/apply-operations-to-make-all-array-elements-equal-to-zero/description/">在线练习</a></td></tr>
</table>

## 题目描述

给你一个下标从 **0** 开始的整数数组 `nums` 和一个正整数 `k` 。
你可以对数组执行下述操作 **任意次** ：
  * 从数组中选出长度为 `k` 的 **任一** 子数组，并将子数组中每个元素都 **减去** `1` 。
如果你可以使数组中的所有元素都等于 `0` ，返回  `true` __ ；否则，返回 __`false` __ 。

**子数组** 是数组中的一个非空连续元素序列。

**示例 1：**

**输入：** nums = [2,2,3,1,1,0], k = 3

**输出：** true

**解释：** 可以执行下述操作：
- 选出子数组 [2,2,3] ，执行操作后，数组变为 nums = [_**1**_ ,_**1**_ ,_**2**_ ,1,1,0] 。
- 选出子数组 [2,1,1] ，执行操作后，数组变为 nums = [1,1,_**1**_ ,_**0**_ ,_**0**_ ,0] 。
- 选出子数组 [1,1,1] ，执行操作后，数组变为 nums = [_**0**_ ,_**0**_ ,_**0**_ ,0,0,0] 。

**示例 2：**

**输入：** nums = [1,3,1,1], k = 2

**输出：** false

**解释：** 无法使数组中的所有元素等于 0 。

**提示：**
  * `1 <= k <= nums.length <= 105`
  * `0 <= nums[i] <= 106`

## 示例

```
[2,2,3,1,1,0]
3
[1,3,1,1]
2
```

## 参考答案

```java
class Solution {
    public boolean checkArray(int[] nums, int k) {
        
    }
}
```

## 一刷思路

```java
class Solution {
    public boolean checkArray(int[] nums, int k) {
        
    }
}
```

## 二刷思路

```java
class Solution {
    public boolean checkArray(int[] nums, int k) {
        
    }
}
```
