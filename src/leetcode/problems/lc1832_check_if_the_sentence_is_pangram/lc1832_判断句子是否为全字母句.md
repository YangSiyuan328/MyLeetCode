# 1832. 判断句子是否为全字母句

<table>
<tr><td><b>难度</b></td><td>Easy</td></tr>
<tr><td><b>标签</b></td><td>哈希表;字符串</td></tr>
<tr><td><b>会员</b></td><td>否</td></tr>
<tr><td><b>链接</b></td><td><a href="https://leetcode.cn/problems/check-if-the-sentence-is-pangram/description/">在线练习</a></td></tr>
</table>

## 题目描述

**全字母句** 指包含英语字母表中每个字母至少一次的句子。
给你一个仅由小写英文字母组成的字符串 `sentence` ，请你判断 `sentence` 是否为 **全字母句** 。
如果是，返回 __`true` ；否则，返回 __`false` 。

**示例 1：**

**输入：** sentence = "thequickbrownfoxjumpsoverthelazydog"

**输出：** true

**解释：**sentence 包含英语字母表中每个字母至少一次。

**示例 2：**

**输入：** sentence = "leetcode"

**输出：** false

**提示：**
  * `1 <= sentence.length <= 1000`
  * `sentence` 由小写英语字母组成

## 示例

```
"thequickbrownfoxjumpsoverthelazydog"
"leetcode"
```

## 参考答案

```java
class Solution {
    public boolean checkIfPangram(String sentence) {
        
    }
}
```

## 一刷思路

```java
class Solution {
    public boolean checkIfPangram(String sentence) {
        
    }
}
```

## 二刷思路

```java
class Solution {
    public boolean checkIfPangram(String sentence) {
        
    }
}
```
