# 2795. 并行执行 Promise 以获取独有的结果

<table>
<tr><td><b>难度</b></td><td>Medium</td></tr>
<tr><td><b>标签</b></td><td></td></tr>
<tr><td><b>会员</b></td><td>是</td></tr>
<tr><td><b>链接</b></td><td><a href="https://leetcode.cn/problems/parallel-execution-of-promises-for-individual-results-retrieval/description/">在线练习</a></td></tr>
</table>

## 题目描述



## 示例

```
[() => new Promise(resolve => setTimeout(() => resolve(15), 100))]
[() => new Promise(resolve => setTimeout(() => resolve(20), 100)), () => new Promise(resolve => setTimeout(() => resolve(15), 100))]
[() => new Promise(resolve => setTimeout(() => resolve(30), 200)), () => new Promise((resolve, reject) => setTimeout(() => reject("Error"), 100))]
```

## 参考答案

```java

```

## 一刷思路

```java

```

## 二刷思路

```java

```
