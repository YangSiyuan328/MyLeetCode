# 324. 摆动排序 II

<table>
<tr><td><b>难度</b></td><td>Medium</td></tr>
<tr><td><b>标签</b></td><td>贪心;数组;分治;快速选择;排序</td></tr>
<tr><td><b>会员</b></td><td>否</td></tr>
<tr><td><b>链接</b></td><td><a href="https://leetcode.cn/problems/wiggle-sort-ii/description/">在线练习</a></td></tr>
</table>

## 题目描述

给你一个整数数组 `nums`，将它重新排列成 `nums[0] < nums[1] > nums[2] < nums[3]...` 的顺序。
你可以假设所有输入数组都可以得到满足题目要求的结果。

**示例 1：**

**输入：** nums = [1,5,1,1,6,4]

**输出：**[1,6,1,5,1,4]

**解释：**[1,4,1,5,1,6] 同样是符合题目要求的结果，可以被判题程序接受。

**示例 2：**

**输入：** nums = [1,3,2,2,3,1]

**输出：**[2,3,1,3,1,2]

**提示：**
  * `1 <= nums.length <= 5 * 104`
  * `0 <= nums[i] <= 5000`
  * 题目数据保证，对于给定的输入 `nums` ，总能产生满足题目要求的结果

**进阶：** 你能用 O(n) 时间复杂度和 / 或原地 O(1) 额外空间来实现吗？

## 示例

```
[1,5,1,1,6,4]
[1,3,2,2,3,1]
```

## 参考答案

```java
class Solution {
    public void wiggleSort(int[] nums) {
        
    }
}
```

## 一刷思路

```java
class Solution {
    public void wiggleSort(int[] nums) {
        
    }
}
```

## 二刷思路

```java
class Solution {
    public void wiggleSort(int[] nums) {
        
    }
}
```
