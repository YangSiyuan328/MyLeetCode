# 3591. 检查元素频次是否为质数

<table>
<tr><td><b>难度</b></td><td>Easy</td></tr>
<tr><td><b>标签</b></td><td>数组;哈希表;数学;计数;数论</td></tr>
<tr><td><b>会员</b></td><td>否</td></tr>
<tr><td><b>链接</b></td><td><a href="https://leetcode.cn/problems/check-if-any-element-has-prime-frequency/description/">在线练习</a></td></tr>
</table>

## 题目描述

给你一个整数数组 `nums`。
如果数组中任一元素的 **频次  **是 **质数** ，返回 `true`；否则，返回 `false`。
元素 `x` 的 **频次  **是它在数组中出现的次数。
质数是一个大于 1 的自然数，并且只有两个因数：1 和它本身。

**示例 1：**

**输入：** nums = [1,2,3,4,5,4]

**输出：** true

**解释：**
数字 4 的频次是 2，而 2 是质数。

**示例 2：**

**输入：** nums = [1,2,3,4,5]

**输出：** false

**解释：**
所有元素的频次都是 1。

**示例 3：**

**输入：** nums = [2,2,2,4,4]

**输出：** true

**解释：**
数字 2 和 4 的频次都是质数。

**提示：**
  * `1 <= nums.length <= 100`
  * `0 <= nums[i] <= 100`

## 示例

```
[1,2,3,4,5,4]
[1,2,3,4,5]
[2,2,2,4,4]
```

## 参考答案

```java
class Solution {
    public boolean checkPrimeFrequency(int[] nums) {
        
    }
}
```

## 一刷思路

```java
class Solution {
    public boolean checkPrimeFrequency(int[] nums) {
        
    }
}
```

## 二刷思路

```java
class Solution {
    public boolean checkPrimeFrequency(int[] nums) {
        
    }
}
```
