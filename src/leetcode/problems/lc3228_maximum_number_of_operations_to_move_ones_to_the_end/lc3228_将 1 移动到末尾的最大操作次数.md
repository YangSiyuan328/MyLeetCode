# 3228. 将 1 移动到末尾的最大操作次数

<table>
<tr><td><b>难度</b></td><td>Medium</td></tr>
<tr><td><b>标签</b></td><td>贪心;字符串;计数</td></tr>
<tr><td><b>会员</b></td><td>否</td></tr>
<tr><td><b>链接</b></td><td><a href="https://leetcode.cn/problems/maximum-number-of-operations-to-move-ones-to-the-end/description/">在线练习</a></td></tr>
</table>

## 题目描述

给你一个 二进制字符串 `s`。
你可以对这个字符串执行 **任意次** 下述操作：
  * 选择字符串中的任一下标 `i`（ `i + 1 < s.length` ），该下标满足 `s[i] == '1'` 且 `s[i + 1] == '0'`。
  * 将字符 `s[i]` 向 **右移** 直到它到达字符串的末端或另一个 `'1'`。例如，对于 `s = "010010"`，如果我们选择 `i = 1`，结果字符串将会是 `s = "0** _001_** 10"`。
返回你能执行的**最大** 操作次数。

**示例 1：**

**输入：** s = "1001101"

**输出：** 4

**解释：**
可以执行以下操作：
  * 选择下标 `i = 0`。结果字符串为 `s = "_**001**_ 1101"`。
  * 选择下标 `i = 4`。结果字符串为 `s = "0011 _**01**_ 1"`。
  * 选择下标 `i = 3`。结果字符串为 `s = "001** _01_** 11"`。
  * 选择下标 `i = 2`。结果字符串为 `s = "00** _01_** 111"`。

**示例 2：**

**输入：** s = "00111"

**输出：** 0

**提示：**
  * `1 <= s.length <= 105`
  * `s[i]` 为 `'0'` 或 `'1'`。

## 示例

```
"1001101"
"00111"
```

## 参考答案

```java
class Solution {
    public int maxOperations(String s) {
        
    }
}
```

## 一刷思路

```java
class Solution {
    public int maxOperations(String s) {
        
    }
}
```

## 二刷思路

```java
class Solution {
    public int maxOperations(String s) {
        
    }
}
```
