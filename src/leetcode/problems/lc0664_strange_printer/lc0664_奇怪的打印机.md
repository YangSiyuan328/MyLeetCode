# 664. 奇怪的打印机

<table>
<tr><td><b>难度</b></td><td>Hard</td></tr>
<tr><td><b>标签</b></td><td>字符串;动态规划</td></tr>
<tr><td><b>会员</b></td><td>否</td></tr>
<tr><td><b>链接</b></td><td><a href="https://leetcode.cn/problems/strange-printer/description/">在线练习</a></td></tr>
</table>

## 题目描述

有台奇怪的打印机有以下两个特殊要求：
  * 打印机每次只能打印由 **同一个字符** 组成的序列。
  * 每次可以在从起始到结束的任意位置打印新字符，并且会覆盖掉原来已有的字符。
给你一个字符串 `s` ，你的任务是计算这个打印机打印它需要的最少打印次数。

**示例 1：**

**输入：** s = "aaabbb"

**输出：** 2

**解释：** 首先打印 "aaa" 然后打印 "bbb"。

**示例 2：**

**输入：** s = "aba"

**输出：** 2

**解释：** 首先打印 "aaa" 然后在第二个位置打印 "b" 覆盖掉原来的字符 'a'。

**提示：**
  * `1 <= s.length <= 100`
  * `s` 由小写英文字母组成

## 示例

```
"aaabbb"
"aba"
```

## 参考答案

```java
class Solution {
    public int strangePrinter(String s) {
        
    }
}
```

## 一刷思路

```java
class Solution {
    public int strangePrinter(String s) {
        
    }
}
```

## 二刷思路

```java
class Solution {
    public int strangePrinter(String s) {
        
    }
}
```
