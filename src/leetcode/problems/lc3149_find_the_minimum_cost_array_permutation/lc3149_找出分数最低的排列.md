# 3149. 找出分数最低的排列

<table>
<tr><td><b>难度</b></td><td>Hard</td></tr>
<tr><td><b>标签</b></td><td>位运算;数组;动态规划;位掩码</td></tr>
<tr><td><b>会员</b></td><td>否</td></tr>
<tr><td><b>链接</b></td><td><a href="https://leetcode.cn/problems/find-the-minimum-cost-array-permutation/description/">在线练习</a></td></tr>
</table>

## 题目描述

给你一个数组 `nums` ，它是 `[0, 1, 2, ..., n - 1]` 的一个排列 。对于任意一个 `[0, 1, 2, ..., n - 1]` 的排列 `perm` ，其 **分数** 定义为：
`score(perm) = |perm[0] - nums[perm[1]]| + |perm[1] - nums[perm[2]]| + ... + |perm[n - 1] - nums[perm[0]]|`
返回具有**最低** 分数的排列 `perm` 。如果存在多个满足题意且分数相等的排列，则返回其中字典序最小的一个。

**示例 1：**

**输入：** nums = [1,0,2]

**输出：**[0,1,2]

**解释：**

**![](https://assets.leetcode.com/uploads/2024/04/04/example0gif.gif)**
字典序最小且分数最低的排列是 `[0,1,2]`。这个排列的分数是 `|0 - 0| + |1 - 2| + |2 - 1| = 2` 。

**示例 2：**

**输入：** nums = [0,2,1]

**输出：**[0,2,1]

**解释：**

**![](https://assets.leetcode.com/uploads/2024/04/04/example1gif.gif)**
字典序最小且分数最低的排列是 `[0,2,1]`。这个排列的分数是 `|0 - 1| + |2 - 2| + |1 - 0| = 2` 。

**提示：**
  * `2 <= n == nums.length <= 14`
  * `nums` 是 `[0, 1, 2, ..., n - 1]` 的一个排列。

## 示例

```
[1,0,2]
[0,2,1]
```

## 参考答案

```java
class Solution {
    public int[] findPermutation(int[] nums) {
        
    }
}
```

## 一刷思路

```java
class Solution {
    public int[] findPermutation(int[] nums) {
        
    }
}
```

## 二刷思路

```java
class Solution {
    public int[] findPermutation(int[] nums) {
        
    }
}
```
