# 1614. 括号的最大嵌套深度

<table>
<tr><td><b>难度</b></td><td>Easy</td></tr>
<tr><td><b>标签</b></td><td>栈;字符串</td></tr>
<tr><td><b>会员</b></td><td>否</td></tr>
<tr><td><b>链接</b></td><td><a href="https://leetcode.cn/problems/maximum-nesting-depth-of-the-parentheses/description/">在线练习</a></td></tr>
</table>

## 题目描述

给定 **有效括号字符串** `s`，返回 `s` 的 **嵌套深度** 。嵌套深度是嵌套括号的 **最大** 数量。

**示例 1：**

**输入：** s = "(1+(2*3)+((**8**)/4))+1"

**输出：** 3

**解释：** 数字 8 在嵌套的 3 层括号中。

**示例 2：**

**输入：** s = "(1)+((2))+(((**3**)))"

**输出：** 3

**解释：** 数字 3 在嵌套的 3 层括号中。

**示例 3：**

**输入：** s = "()(())((()()))"

**输出：** 3

**提示：**
  * `1 <= s.length <= 100`
  * `s` 由数字 `0-9` 和字符 `'+'`、`'-'`、`'*'`、`'/'`、`'('`、`')'` 组成
  * 题目数据保证括号字符串 `s` 是 **有效的括号字符串**

## 示例

```
"(1+(2*3)+((8)/4))+1"
"(1)+((2))+(((3)))"
"()(())((()()))"
```

## 参考答案

```java
class Solution {
    public int maxDepth(String s) {
        
    }
}
```

## 一刷思路

```java
class Solution {
    public int maxDepth(String s) {
        
    }
}
```

## 二刷思路

```java
class Solution {
    public int maxDepth(String s) {
        
    }
}
```
