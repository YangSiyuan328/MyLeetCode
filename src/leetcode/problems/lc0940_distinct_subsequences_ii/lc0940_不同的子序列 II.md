# 940. 不同的子序列 II

<table>
<tr><td><b>难度</b></td><td>Hard</td></tr>
<tr><td><b>标签</b></td><td>字符串;动态规划</td></tr>
<tr><td><b>会员</b></td><td>否</td></tr>
<tr><td><b>链接</b></td><td><a href="https://leetcode.cn/problems/distinct-subsequences-ii/description/">在线练习</a></td></tr>
</table>

## 题目描述

给定一个字符串 `s`，计算 `s` 的 **不同非空子序列** 的个数。因为结果可能很大，所以返回答案需要对******`10^9 + 7` 取余** 。
字符串的 **子序列** 是经由原字符串删除一些（也可能不删除）字符但不改变剩余字符相对位置的一个新字符串。
  * 例如，`"ace"` 是 `"_**a**_ b _**c**_ d _**e**_ "` 的一个子序列，但 `"aec"` 不是。

**示例 1：**

**输入：** s = "abc"

**输出：** 7

**解释：** 7 个不同的子序列分别是 "a", "b", "c", "ab", "ac", "bc", 以及 "abc"。

**示例 2：**

**输入：** s = "aba"

**输出：** 6

**解释：** 6 个不同的子序列分别是 "a", "b", "ab", "ba", "aa" 以及 "aba"。

**示例 3：**

**输入：** s = "aaa"

**输出：** 3

**解释：** 3 个不同的子序列分别是 "a", "aa" 以及 "aaa"。

**提示：**
  * `1 <= s.length <= 2000`
  * `s` 仅由小写英文字母组成

## 示例

```
"abc"
"aba"
"aaa"
```

## 参考答案

```java
class Solution {
    public int distinctSubseqII(String s) {
        
    }
}
```

## 一刷思路

```java
class Solution {
    public int distinctSubseqII(String s) {
        
    }
}
```

## 二刷思路

```java
class Solution {
    public int distinctSubseqII(String s) {
        
    }
}
```
