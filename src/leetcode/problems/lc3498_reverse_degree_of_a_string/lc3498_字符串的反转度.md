# 3498. 字符串的反转度

<table>
<tr><td><b>难度</b></td><td>Easy</td></tr>
<tr><td><b>标签</b></td><td>字符串;模拟</td></tr>
<tr><td><b>会员</b></td><td>否</td></tr>
<tr><td><b>链接</b></td><td><a href="https://leetcode.cn/problems/reverse-degree-of-a-string/description/">在线练习</a></td></tr>
</table>

## 题目描述

给你一个字符串 `s`，计算其 **反转度** 。

**反转度** 的计算方法如下：
  1. 对于每个字符，将其在 **反转** 字母表中的位置（`'a'` = 26, `'b'` = 25, ..., `'z'` = 1）与其在字符串中的位置（下标从**1** 开始）相乘。
  2. 将这些乘积加起来，得到字符串中所有字符的和。
返回 **反转度** 。

**示例 1：**

**输入：** s = "abc"

**输出：** 148

**解释：**
字母 | 反转字母表中的位置 | 字符串中的位置 | 乘积  
---|---|---|---  
`'a'` | 26 | 1 | 26  
`'b'` | 25 | 2 | 50  
`'c'` | 24 | 3 | 72  
反转度是 `26 + 50 + 72 = 148` 。

**示例 2：**

**输入：** s = "zaza"

**输出：** 160

**解释：**
字母 | 反转字母表中的位置 | 字符串中的位置 | 乘积  
---|---|---|---  
`'z'` | 1 | 1 | 1  
`'a'` | 26 | 2 | 52  
`'z'` | 1 | 3 | 3  
`'a'` | 26 | 4 | 104  
反转度是 `1 + 52 + 3 + 104 = 160` 。

**提示：**
  * `1 <= s.length <= 1000`
  * `s` 仅包含小写字母。

## 示例

```
"abc"
"zaza"
```

## 参考答案

```java
class Solution {
    public int reverseDegree(String s) {
        
    }
}
```

## 一刷思路

```java
class Solution {
    public int reverseDegree(String s) {
        
    }
}
```

## 二刷思路

```java
class Solution {
    public int reverseDegree(String s) {
        
    }
}
```
