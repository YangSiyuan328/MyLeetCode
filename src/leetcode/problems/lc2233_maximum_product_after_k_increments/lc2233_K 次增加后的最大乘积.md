# 2233. K 次增加后的最大乘积

<table>
<tr><td><b>难度</b></td><td>Medium</td></tr>
<tr><td><b>标签</b></td><td>贪心;数组;堆（优先队列）</td></tr>
<tr><td><b>会员</b></td><td>否</td></tr>
<tr><td><b>链接</b></td><td><a href="https://leetcode.cn/problems/maximum-product-after-k-increments/description/">在线练习</a></td></tr>
</table>

## 题目描述

给你一个非负整数数组 `nums` 和一个整数 `k` 。每次操作，你可以选择 `nums` 中 **任一**  元素并将它 **增加**  `1` 。
请你返回 **至多**  `k` 次操作后，能得到的 _ _`nums`的 **最大乘积**  。由于答案可能很大，请你将答案对 `109 + 7` 取余后返回。

**示例 1：**

**输入：** nums = [0,4], k = 5

**输出：** 20

**解释：** 将第一个数增加 5 次。
得到 nums = [5, 4] ，乘积为 5 * 4 = 20 。
可以证明 20 是能得到的最大乘积，所以我们返回 20 。
存在其他增加 nums 的方法，也能得到最大乘积。

**示例 2：**

**输入：** nums = [6,3,3,2], k = 2

**输出：** 216

**解释：** 将第二个数增加 1 次，将第四个数增加 1 次。
得到 nums = [6, 4, 3, 3] ，乘积为 6 * 4 * 3 * 3 = 216 。
可以证明 216 是能得到的最大乘积，所以我们返回 216 。
存在其他增加 nums 的方法，也能得到最大乘积。

**提示：**
  * `1 <= nums.length, k <= 105`
  * `0 <= nums[i] <= 106`

## 示例

```
[0,4]
5
[6,3,3,2]
2
```

## 参考答案

```java
class Solution {
    public int maximumProduct(int[] nums, int k) {
        
    }
}
```

## 一刷思路

```java
class Solution {
    public int maximumProduct(int[] nums, int k) {
        
    }
}
```

## 二刷思路

```java
class Solution {
    public int maximumProduct(int[] nums, int k) {
        
    }
}
```
