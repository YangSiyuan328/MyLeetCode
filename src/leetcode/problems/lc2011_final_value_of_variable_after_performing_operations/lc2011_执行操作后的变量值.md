# 2011. 执行操作后的变量值

<table>
<tr><td><b>难度</b></td><td>Easy</td></tr>
<tr><td><b>标签</b></td><td>数组;字符串;模拟</td></tr>
<tr><td><b>会员</b></td><td>否</td></tr>
<tr><td><b>链接</b></td><td><a href="https://leetcode.cn/problems/final-value-of-variable-after-performing-operations/description/">在线练习</a></td></tr>
</table>

## 题目描述

存在一种仅支持 4 种操作和 1 个变量 `X` 的编程语言：
  * `++X` 和 `X++` 使变量 `X` 的值 **加** `1`
  * `--X` 和 `X--` 使变量 `X` 的值 **减** `1`
最初，`X` 的值是 `0`
给你一个字符串数组 `operations` ，这是由操作组成的一个列表，返回执行所有操作后， __`X` 的 **最终值** 。

**示例 1：**

**输入：** operations = ["--X","X++","X++"]

**输出：** 1

**解释：** 操作按下述步骤执行：
最初，X = 0
--X：X 减 1 ，X =  0 - 1 = -1
X++：X 加 1 ，X = -1 + 1 =  0
X++：X 加 1 ，X =  0 + 1 =  1

**示例 2：**

**输入：** operations = ["++X","++X","X++"]

**输出：** 3

**解释：** 操作按下述步骤执行： 
最初，X = 0
++X：X 加 1 ，X = 0 + 1 = 1
++X：X 加 1 ，X = 1 + 1 = 2
X++：X 加 1 ，X = 2 + 1 = 3

**示例 3：**

**输入：** operations = ["X++","++X","--X","X--"]

**输出：** 0

**解释：** 操作按下述步骤执行：
最初，X = 0
X++：X 加 1 ，X = 0 + 1 = 1
++X：X 加 1 ，X = 1 + 1 = 2
--X：X 减 1 ，X = 2 - 1 = 1
X--：X 减 1 ，X = 1 - 1 = 0

**提示：**
  * `1 <= operations.length <= 100`
  * `operations[i]` 将会是 `"++X"`、`"X++"`、`"--X"` 或 `"X--"`

## 示例

```
["--X","X++","X++"]
["++X","++X","X++"]
["X++","++X","--X","X--"]
```

## 参考答案

```java
class Solution {
    public int finalValueAfterOperations(String[] operations) {
        
    }
}
```

## 一刷思路

```java
class Solution {
    public int finalValueAfterOperations(String[] operations) {
        
    }
}
```

## 二刷思路

```java
class Solution {
    public int finalValueAfterOperations(String[] operations) {
        
    }
}
```
